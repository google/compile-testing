language: java
matrix:
  include:
    - os: linux
      arch: amd64
      jdk: openjdk11
    - os: linux
      arch: ppc64le
      jdk: openjdk11
      
before_script:
  - java -version
  - mvn -version
  
script:
  - mvn test   
  
before_install:
 
