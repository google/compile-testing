language: java
matrix:
  include:
    - os: linux
      arch: amd64
      jdk: openjdk8
    - os: linux
      arch: ppc64le
      jdk: openjdk8
      
before_script:
  - java -version
  - mvn -version
  
script:
  - mvn test 
  - mvn ekstazi:select
  
before_install:
 
